/** @type {import('next').NextConfig} */
const nextConfig = {
  output: 'export',
  basePath: '/dashboard-projecao-aeroporto',
  images: {
    unoptimized: true,
  },
};

module.exports = nextConfig;
